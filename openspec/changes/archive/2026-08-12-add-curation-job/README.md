# add-curation-job

The daily curation job (intent US-002): scheduled and POST /curate-triggered orchestration that fetches feeds, curates, classifies longevity via pi, and stores records split into news/knowledge streams; adds sources+schedule config and a thread-safe store.
