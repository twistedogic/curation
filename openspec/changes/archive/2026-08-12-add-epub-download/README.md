# add-epub-download

Per-kind incremental EPUB download via kind-scoped tokens, a stdlib-only EPUB builder (std.zip headers + std.compress.flate), and the bearer-authed GET /download endpoint returning the EPUB or 204 No Content.
